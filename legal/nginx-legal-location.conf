# Add inside the HTTPS server { ... } block for api.newlanebrokers.com
# (usually /etc/nginx/sites-available/api.newlanebrokers.com)

    location /legal/ {
        alias /var/www/html/legal/;
        index index.html;
        try_files $uri $uri/ =404;
    }
