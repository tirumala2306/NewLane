rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV ONLY — tighten before production (Firebase Auth custom tokens).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
